import React from 'react';

import { withDisplayName } from 'src/common/components/utils/wrappers';

const LocationInput: React.FC = () => {
  return (
    <div>
      <input />
    </div>
  );
}

export default withDisplayName(LocationInput);
